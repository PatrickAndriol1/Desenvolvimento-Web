# Funções em JS
    uma função executa uma sequência de instruções
# Diferença entre *var* e *let*
    var é usado para criar variáveis de escopo global (não respeito o escopo)
    let é usado para criar variáveis em escopo local
